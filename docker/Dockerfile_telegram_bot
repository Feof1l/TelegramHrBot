# Используйте образ Golang для сборки
FROM golang:latest

# Установите рабочую директорию внутри контейнера
WORKDIR /PmiiHRbot

# Скопируйте содержимое вашего приложения в контейнер
COPY . .

# Установите зависимости, если это необходимо
RUN go mod tidy

# Соберите ваше приложение
 RUN go build -o main .

# Укажите порт, который ваше приложение будет использовать
 EXPOSE 8080

# Укажите команду для запуска вашего приложения
 CMD ["./main"]
